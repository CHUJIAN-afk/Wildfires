pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = 'MinecraftForge'
            url = 'https://maven.minecraftforge.net/'
        }
    }
    repositories {
        maven { url = 'https://maven.parchmentmc.org' } // Add this line
    }
}

// The project supplies its Java toolchain through JAVA_HOME/IDE settings.
// Keep startup independent of the optional Foojay plugin and its download.
// plugins {
//     id 'org.gradle.toolchains.foojay-resolver-convention' version '0.7.0'
// }

rootProject.name = 'wildfires'
